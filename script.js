/**
 * * START
 * * PROGRAM getTheBall
 * * Create variable isHandOpen
 * * Create variable elbowAngle
 * * Set isHandOpen to true
 * * Set elbowAngle to 0
 * 
 * * Set elbowAngle to 105
 * * Move elbowjoint to elbowAngle
 * * Set isHandOpen to false
 * * Set elbowAngle to 0
 * * Move elbowJoint to elboAngle
 * * END
 */

// ** Strings, numbers, booleans, array, object

// ** let and const

let faveFood1 = "Biryani";
console.log(faveFood1);
let faveFood2 = 'Pastizzi';
console.log(faveFood2);

// * template literal
let foodsToTry = `You should try ${faveFood1} and ${faveFood2} they are delicious!`;
console.log(foodsToTry);

// * Booleans
let dinosaursDontExistAnymore = 1;
let earthIsFlat = 0;

// * numbers
// * 5 is an integer
// * 3.14159 is a float
let myFaveInteger = 5;
let myFaveFloat = 3.14;

let strangeCarSetupWheels = "3";
console.log(typeof(strangeCarSetupWheels));
console.log(typeof(parseInt(strangeCarSetupWheels)));


